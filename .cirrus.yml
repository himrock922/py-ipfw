freebsd_instance:
  image: freebsd-12-0-release-amd64

task:
  install_script:
    - pkg update
    - pkg install -y qjail
  qjail_script:
    - qjail install
    - qjail create -4 192.168.0.2 jail1
    - qjail config -w vtnet0 -v ipfw jail1
    - qjail start jail1
    - ifconfig -a
  script:
    - qjail console jail1
    - ifconfig -a
    - ipfw list
    - exit
